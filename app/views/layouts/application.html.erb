<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) || "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
	  <div id="user_nav">
        <% if current_user %>
          You're logged in as <%= current_user.name %>. Not you?
          <%= link_to "Sign Out", signout_path %>
        <% else %>
          <%= link_to "Sign in with Facebook", "/auth/facebook" %>
        <% end %>
      </div>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
    </div>
	<div id= "content">
	  <br>	
	  <h2>
	    Set up a sublet with people you know!
	  </h2>
	  <br>
	  <br>
	  <h3>
	    Looking for a friend to sublet from?
	  </h3>
	  <p>
		TODO: XX of your friends are already using Sublettuce. 
		Sign in with Facebook to see which friends have posts.
		
	  <p>
	  <br>
	  <h3>
	    Tell your friends about your sweet place!
	  </h3>
	  <p>
	    TODO Button: Make a post, and share it!
	  </p>
	</div>
	

</body>
</html>
